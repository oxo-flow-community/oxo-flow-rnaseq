# Environment for the reference-chain Python scripts (gtffilter.py,
# fasta2gtf.py; stdlib-only). Pinned to the upstream nf-core/rnaseq 3.26.0
# module environments (custom/catadditionalfasta python=3.12.2;
# custom/gtffilter pins python=3.14.5 — both only need the stdlib).
name: python
channels:
  - conda-forge
  - bioconda
dependencies:
  - conda-forge::python=3.12.2
