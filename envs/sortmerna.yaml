# Pinned exactly to the upstream nf-core/rnaseq 3.26.0 SORTMERNA module
# environment (modules/nf-core/sortmerna/environment.yml).
name: sortmerna
channels:
  - conda-forge
  - bioconda
dependencies:
  - conda-forge::sortmerna=4.3.7
