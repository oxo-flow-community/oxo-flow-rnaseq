# Pinned exactly to the upstream nf-core/rnaseq 3.26.0 RSEM modules
# environment (modules/nf-core/rsem/calculateexpression/environment.yml).
name: rsem
channels:
  - conda-forge
  - bioconda
dependencies:
  - bioconda::rsem=1.3.3
