---
# Environment for bracken — pinned exactly to the upstream
# nf-core/rnaseq 3.26.0 module environment.
name: bracken
channels:
  - conda-forge
  - bioconda
dependencies:
  - bioconda::bracken=3.1
