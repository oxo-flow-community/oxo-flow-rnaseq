---
# Environment for kraken2 — pinned exactly to the upstream
# nf-core/rnaseq 3.26.0 module environment.
name: kraken2
channels:
  - conda-forge
  - bioconda
dependencies:
  - bioconda::kraken2=2.1.6
